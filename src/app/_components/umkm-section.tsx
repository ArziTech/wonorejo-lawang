import SectionHeading from "@/components/global/section-heading";
import {Star} from "lucide-react";

const UmkmSection = () => {
  return (
      <div className={'container mx-auto'}>
        <SectionHeading title={'UMKM di Wonorejo'} Icon={Star} />
      </div>
  );
};
export default UmkmSection;
